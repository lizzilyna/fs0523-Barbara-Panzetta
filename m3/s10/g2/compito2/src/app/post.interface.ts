export interface Post {
    id: number;
    title: string;
    active: boolean;
  }

  // in src/app creo un'interfaccia chiamata Post che rappresenta la struttura di un oggetto post.
  // Un flag booleano indica se il post è attivo o inattivo.